// [SectionCard] moved into the shared kit when the whole interface was
// rebuilt, so there is one definition rather than two that drift apart.
// This file stays as the export the screens already import.
export 'ui_kit.dart' show SectionCard;
